# CLAUDE.md

قواعد دائمة لهذا المستودع. التفاصيل في الملفات المحال إليها، ولا تُنسخ هنا.

## ما هو هذا المستودع
موقع الكورس الذي يصل إلى المشترك: منصة تعلم عربية للويب، مبنية بـAstro بإخراج ثابت بالكامل.
هذا المستودع هو **موقع المشتري وحده**.

## قواعد لا تُخالف
- **ممنوع** أي ملف تسويقي أو داخلي: `marketing` · `owner` · `research` · `sources` · `claims` · تسعير · خطط بيع · بيانات جمهور · محادثات. يفشل البناء عند ظهورها (`scripts/check-forbidden.mjs`)
- **تصميم ويب أصلي**: لا شكل صفحات ورقية، ولا أرقام صفحات، ولا واجهة تشبه عارض ملفات
- **الهاتف أولا**: يُختبر على 360 و390 و412 قبل سطح المكتب. مساحة اللمس ≥ 48px
- **RTL** مع `lang="ar"` و`dir="rtl"`، وخصائص CSS المنطقية، وعزل الإنجليزية والأرقام بـ`bdi` أو `.num`
- **الخطوط**: ثلاث عائلات محلية فقط — Noto Naskh Arabic للمتن، Readex Pro للواجهة، Alexandria للعناوين. لا طلب شبكة خارجي
- **خلفيات فاتحة متنوعة**: لا أبيض خالص ولا واجهة داكنة. لكل مرحلة لون ثابت من `src/styles/tokens.css`
- **النبرة بشرية**: مدرب خبير يشرح لمبتدئ. الممنوعات اللغوية في `scripts/lint.mjs`
- **الأرقام لاتينية** فقط: 0 1 2 3
- **الرابط الخارجي الوحيد**: `https://t.me/PromptsArabic` باسم `@PromptsArabic`، في ثلاثة مواضع مقصودة لا أكثر
- **الاختبارات قبل النشر**: `npm run qa` يجب أن ينجح كاملا. نجاح البناء وحده ليس دليلا

## الأوامر
```
npm run dev        تطوير
npm run build      بناء الإنتاج
npm run preview    تقديم dist تحت المسار الفرعي الحقيقي
npm run qa         كل الفحوص والاختبارات
```

## المراجع
`PRODUCT_WEB.md` · `INFORMATION_ARCHITECTURE.md` · `CONTENT_MODEL.md` · `DESIGN_SYSTEM_WEB.md` · `DESIGN_DECISIONS.md` · `DECISIONS.md` · `STATE.md` · `QA_REPORT.md`

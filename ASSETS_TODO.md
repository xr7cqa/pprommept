# الأصول البصرية المطلوبة

ملف إدارة داخلي. لا يدخل ناتج الموقع — فحص الملفات الممنوعة يمنعه في `dist`.

## المبدأ المطبق

كل رسم في الموقع مصنوع بـSVG مكتوب بخط اليد داخل `src/components/content/Diagram.astro`، أو بـCSS داخل مكونات المحتوى. لا صورة نقطية واحدة في الموقع، ولا طلب شبكة خارجي.

النص العربي داخل المخططات نص حقيقي في وسم `text`، لا صورة. هذا يجعله مقروءا لقارئ الشاشة، وقابلا للبحث، وحادا على كل كثافات الشاشات، ويتجنب مشكلة تقطّع الحروف العربية في الصور المولدة.

## ما أُنتج فعلا في هذه الجولة

| الأصل | النوع | أين يُستعمل |
|---|---|---|
| `Diagram kind="delegation"` | SVG أصلي | درس ما تفعله الأداة — سلّم التفويض بأربع درجات |
| `Diagram kind="layers"` | SVG أصلي | فهرس التفكيكات التطبيقية — الطبقات الثلاث |
| مكوّن `Layer` بثلاثة أشكال | CSS | كل صفحات التفكيكات التطبيقية |
| مكوّن `Field` | CSS | الاثنا عشر قالبا التفاعلي |

الأشكال الثلاثة في مكوّن `Layer` تحمل معنى لا زينة: الطبقة الموثقة لها خط جانبي سميك مصمت، وطبقة القراءة لها حد متقطع يشير إلى أنها غير مثبتة، وطبقة المبدأ لها زاوية مقطوعة وهي الشكل المخصص للخلاصات في هذا الموقع. القارئ يميّز نوع الطبقة قبل أن يقرأ عنوانها.

## ما لم يُنتج ولماذا

هذه أصول قد تحسّن الفهم ولم تُنتج، وكل واحد مذكور مع سبب واضح وبرومبت جاهز لمن أراد إنتاجه لاحقا بأداة توليد.

القاعدة الحاكمة لكل ما يلي: **لا يُكتب نص عربي داخل أي صورة مولدة**. أدوات التوليد تتعامل مع العربية كأشكال لا كحروف، فتخرج مقطّعة أو مقلوبة. النص يُضاف في طبقة التصميم بعد التوليد، أو يُكتب في وسم `text` داخل SVG.

### 1. صورة غلاف للتفكيكات التطبيقية

السبب في عدم الإنتاج: تحتاج توليدا نقطيا، وهو خارج ما يُنتج بـSVG يدويا، والموقع بلا صور نقطية أصلا.

البرومبت الجاهز:

> A quiet flat-lay of an open notebook on a warm beige desk, three sticky notes of different colors placed in a vertical column beside it, soft daylight from a side window, muted earth-tone palette, no text anywhere in the image, no people, no logos, photographic, generous empty space in the upper third for a caption to be added later.

### 2. لقطات توضيحية لواجهات المنصات

السبب في عدم الإنتاج: لقطة شاشة حقيقية لأي منصة تحمل علامتها التجارية وواجهتها، وهي مادة محمية، ولا تُستعمل. والرسم التخطيطي البديل يؤدي الغرض بلا هذه المشكلة.

البديل المطبق: وصف الترتيب بالنص وبالجداول، وهو ما اعتُمد في صفحات المنصات فعلا.

### 3. صور «قبل وبعد» لحالات حقيقية

السبب في عدم الإنتاج: أي صورة تُعرض على أنها عمل حقيقي وليست كذلك تكون ادعاء كاذبا، وهذا خط لا يُتجاوز في هذا الكورس.

البديل المطبق: مكوّن `Compare` يعرض النص قبل وبعد بلا صورة، مع تشخيص مكتوب للفرق. وهو أنفع للمتعلم لأنه يُظهر سبب الفرق لا شكله.

### 4. أيقونات إضافية للتصنيفات الجديدة

السبب في عدم الإنتاج: مجموعة الأيقونات الحالية في `Icon.astro` تغطي الاستعمالات القائمة، وإضافة أيقونات جديدة الآن تزيد وزن الصفحة بلا فائدة واضحة.

يُعاد النظر إن أضيفت مجموعة محتوى جديدة تحتاج تمييزا بصريا في التنقل.

## قواعد ملزمة لأي أصل يُضاف لاحقا

- لا صورة تُقدَّم على أنها توثيق لشيء لم يحدث
- لا صورة لشخص حقيقي بلا ترخيص، ولا صورة تشبه شخصا معروفا
- لا نص عربي داخل صورة مولدة
- لا طلب شبكة خارجي: كل أصل يُدمج في المستودع
- كل SVG يحمل `role="img"` و`aria-label` عربيا يصف ما فيه، و`title` داخليا
- الألوان تُؤخذ من `src/styles/tokens.css` ولا تُكتب قيما ثابتة جديدة إلا داخل مخططات SVG التي تحتاج تباينا محسوبا

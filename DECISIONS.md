# DECISIONS.md — القرارات التقنية وأسبابها

الحالة: `مثبت` = قرار نهائي · `افتراض` = أفضل حكم متاح ويمكن تغييره · `عائق` = يحتاج إجراء خارج المستودع

---

## W-001 — Astro 7.2.2 بإخراج ثابت — `مثبت`
تُحقق من الإصدار المستقر وقت التنفيذ لا من رقم مكتوب مسبقا. كل درس صفحة HTML حقيقية،
والمحتوى في Content Collections، وبلا إطار واجهة لأن Astro وTypeScript الخفيف يكفيان.
لا خادم ولا قاعدة بيانات ولا API وقت التشغيل.

## W-002 — المسار الأساسي يُشتق من اسم المستودع — `مثبت` (كان عائقا وحُسم)
**ما كان**: التكليف يذكر مستودعا باسم `pprommept`، والموجود وقت البناء كان `pprommept-`
بشرطة في آخره. بُني الموقع على `/pprommept-/` حتى يعمل فعليا، مع جعل المسار متغيرا واحدا.

**ما صار**: أُعيدت تسمية المستودع إلى `pprommept`، فصار الرابط المطلوب
`https://xr7cqa.github.io/pprommept/` هو الرابط الفعلي بلا تعديل في الكود.

**القرار الدائم**: في سير النشر يُشتق المسار من اسم المستودع
(`/${{ github.event.repository.name }}/`)، ومحليا قيمته الافتراضية `/pprommept/` وتُتجاوز
بـ`SITE_BASE`. لا يُكتب المسار بنصه في أي ملف — بما في ذلك الاختبارات، لأن كتابته في
اختبار الواجهة هي ما أوقف أول نشر بعد إعادة التسمية.

## W-003 — فهرس بحث محلي بدل مكتبة جاهزة — `مثبت`
جُرّبت الحاجة إلى تطبيع عربي نتحكم فيه: الهمزات، والألف المقصورة، والتاء المربوطة، والتشكيل.
المكتبات الجاهزة تضيف WebAssembly وسلوك تقطيع لا نتحكم فيه للعربية.
البديل المعتمد: فهرس JSON يُبنى وقت البناء ويُحمَّل عند فتح صفحة البحث وحدها.
الحجم بعد التقليم 303KB خاما و71KB مضغوطا، ولا يُحمَّل في أي صفحة أخرى.
التطبيق يُغيّر نص المطابقة فقط ولا يمس النص الظاهر للمستخدم.

## W-004 — الخطوط الثلاثة — `مثبت`
Noto Naskh Arabic للمتن · Readex Pro للواجهة · Alexandria للعناوين. كلها OFL-1.1 ومحلية.
**ملاحظة**: تحفظ سابق على Noto Naskh كان بسبب تفتّت الكلمات عند استخراج النص من ملفات PDF.
هذا القيد لا ينطبق على الويب: النص هنا HTML حقيقي، والنسخ والبحث يعملان، وقد اختُبرا فعلا.

## W-005 — إصدار بيانات التخزين المحلي — `مثبت`
مفتاح واحد `pa.course` بحقل `v`. الترقية من إصدار إلى إصدار تحفظ كل الحقول السابقة،
والبيانات التالفة تُستبدل بقيم سليمة بدل تعطيل الصفحة، وتعذّر التخزين يعطّل ميزات التقدم
بلطف ويبقي المحتوى والتنقل يعملان. مغطّى باختبار ترقية من الإصدار 1 إلى 2.

## W-006 — قائمة المحتويات تُبنى مفتوحة ثم تُطوى على الهاتف — `مثبت`
جُرّب إبقاؤها مطوية وإظهارها على سطح المكتب بـCSS، ففشل لأن المتصفح يخفي محتوى `details`
بطريقة لا تُتجاوز بقاعدة عرض. القرار: تُبنى مفتوحة فتعمل بلا جافاسكربت، ويطويها الجافاسكربت
على الشاشات الضيقة وحدها توفيرا للمساحة.

## W-007 — شرح المصطلح داخل الصفحة — `مثبت`
المصطلح رابط عادي إلى القاموس، والجافاسكربت يعترضه ويفتح لوحة سفلية على الهاتف ونافذة على
سطح المكتب. الفائدة: يعمل بلا جافاسكربت، ولا يغادر المتعلم الدرس حين يكون الجافاسكربت يعمل،
ولا يُحمَّل ملف بيانات إضافي لأن الشرح المختصر مخزون في سمات الرابط نفسه.

## W-008 — لا Service Worker في الإصدار الأول — `مثبت`
العمل بلا اتصال يحتاج تخزينا مؤقتا ذا إصدار واختبارات تمنع بقاء نسخة قديمة بعد تحديث الكورس.
الموقع اليوم خفيف (19.5KB جافاسكربت لكل الصفحات)، والمكسب لا يوازي خطر تقديم درس قديم لمتعلم.
يُعاد النظر عند وجود اختبارات تحديث موثوقة.

## W-009 — استئناف القراءة باختيار المتعلم — `مثبت`
التمرير التلقائي إلى الموضع المحفوظ يربك من فتح الدرس ليقرأه من أوله. البديل: شريحة صغيرة
تظهر عند العودة وتختفي بعد تسع ثوان، والانتقال يحدث بضغطة.

## W-010 — إعادة الضبط في الإعدادات وحدها — `مثبت`
زر مسح دائم في لوحة المتعلم يُضغط خطأ. نُقل إلى صفحة إعدادات مستقلة مع تأكيد يذكر عدد
العناصر التي ستُمسح.

## W-011 — `[hidden]` بأولوية قصوى — `مثبت`
قواعد العرض في المكونات (`display:flex` وغيرها) كانت تتجاوز السمة فتظهر عناصر يفترض إخفاؤها.
أُضيفت قاعدة عامة، ومعها اختبار يمر على الصفحات ويفشل عند أي عنصر مخفي ظاهر بصريا.

## W-012 — المعلومات المتغيرة في ملحق مؤرخ بلا أرقام — `مثبت`
أسماء خطط أدوات الذكاء الاصطناعي وأسعارها وحدودها تتغير كل بضعة أشهر. ولأن الموقع لا يسمح
بروابط خارجية، فالقارئ لا يستطيع التحقق من رقم نكتبه. القرار: الملحق يشرح **ما الذي يتغير
وكيف يتحقق منه المتعلم بنفسه داخل التطبيق**، بلا أرقام نضمنها ولا نستطيع تحديثها.

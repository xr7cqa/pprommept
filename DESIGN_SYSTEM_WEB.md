# DESIGN_SYSTEM_WEB.md — نظام التصميم

كل القيم في `src/styles/tokens.css`. ممنوع كتابة قيمة لونية أو مقاسية داخل مكون.

## المبدأ
التنوع منظم لا عشوائي: لكل مرحلة لون ثابت، ولكل وظيفة شكل بطاقة مختلف. القارئ يتعلم التنقل
بعينه قبل أن يقرأ حرفا.

## الخلفيات
- الخلفية العامة `--paper: #FBF6EE` — عاجي دافئ، ليست بيضاء
- لكل مرحلة `--sN-bg` و`--sN-ink`: رملي · أزرق ضبابي · أخضر مريمي · لافندر · أصفر كريمي · فيروزي · مرجاني
- لا أكثر من ثلاثة ألوان مسيطرة في الشاشة الواحدة
- المتن الطويل بلون `--ink` دائما مهما تغيرت الخلفية

## البطاقات — الشكل يتبع الوظيفة
| الوظيفة | الصنف | المعالجة |
|---|---|---|
| مثال | `.box-example` | مغمور بلون كامل بلا إطار |
| تطبيق | `.box-apply` | خط جانبي عريض |
| تنبيه وخطأ شائع | `.box-warn` | زاوية مقصوصة، غير مستطيل |
| ملاحظة خبير | `.box-note` | بلا صندوق، علامة على الحافة |
| نتيجة يملكها المتعلم | `.box-result` | داكن بنص فاتح |
| قائمة فحص | `.box-check` | سطح هادئ ومربعات قابلة للتحديد |
| برومبت | `.prompt-card` | حدود متقطعة تشبه حقل إدخال |
| خطوات | `.flow` | أرقام كبيرة بلا صندوق |
| اقتباس محوري | `.pull` | مساحة مفتوحة بخطين |
| مقارنة قبل وبعد | `.compare` | عمودان وشريط تشخيص داكن |

## الخطوط
| الدور | الخط | المتغير |
|---|---|---|
| المتن الطويل | Noto Naskh Arabic | `--f-body` |
| الواجهة والبطاقات | Readex Pro | `--f-ui` |
| العناوين الكبرى | Alexandria | `--f-display` |

كلها WOFF2 محلية في `src/assets/fonts/` مع تراخيصها (OFL-1.1)، ومقسّمة بنطاق عربي ولاتيني.

**قيود**: لا خط كوفي ولا تاريخي للمتن · لا `letter-spacing` على العربية · لا `text-align: justify` ·
`font-synthesis: none` · لا نص أساسي تحت 16px · متن الدرس 19px بارتفاع سطر 1.9.

## المقاسات
`--t-xs 13px` · `--t-sm 16px` · `--t-base 18px` · `--t-lg 19px` · العناوين بـ`clamp()`
من 30px إلى 46px للرئيسي، ومن 23px إلى 32px للفرعي.

## الحركة
120ms إلى 220ms عند الحاجة فقط، ويُحترم `prefers-reduced-motion` بإيقاف كامل.

## التباين
يُحسب برمجيا في `scripts/check-contrast.mjs` على 47 تركيبة مستخدمة فعلا:
المتن ≥ 7:1، والنص الثانوي والعناوين الملونة ≥ 4.5:1. الفشل يوقف البناء.
